sap.ui.define([
    "sap/ui/test/opaQunit"
], function (opaTest) {
    "use strict";

    var Journey = {
        run: function() {
            QUnit.module("First journey");

            opaTest("Start application", function (Given, When, Then) {
                Given.iStartMyApp();

                Then.onThes4cloudsuccessorsList.iSeeThisPage();

            });


            opaTest("Navigate to ObjectPage", function (Given, When, Then) {
                // Note: this test will fail if the ListReport page doesn't show any data
                When.onThes4cloudsuccessorsList.onFilterBar().iExecuteSearch();
                Then.onThes4cloudsuccessorsList.onTable().iCheckRows();

                When.onThes4cloudsuccessorsList.onTable().iPressRow(0);
                Then.onThes4cloudsuccessorsObjectPage.iSeeThisPage();

            });

            opaTest("Teardown", function (Given, When, Then) { 
                // Cleanup
                Given.iTearDownMyApp();
            });
        }
    }

    return Journey;
});